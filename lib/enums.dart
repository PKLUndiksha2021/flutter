// enum MenuCustomerState { dashboard, ticket, company, profile, logout }
// enum MenuDimataState { dashboard, ticket, company, profile, logout }
// enum MenuAdminState { dashboard, ticket, company, user, comment, profile, logout }
